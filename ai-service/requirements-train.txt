# Extra packages for QLoRA fine-tune only — do not install into the API runtime.
# Use a separate venv if the API .venv must stay lean:
#   python -m venv .venv-train
#   .venv-train\Scripts\pip install -r requirements-train.txt
#
# Unsloth is preferred on NVIDIA; the trainer falls back to PEFT + bitsandbytes.

torch>=2.4.0
transformers>=4.45.0
datasets>=3.0.0
accelerate>=1.0.0
peft>=0.13.0
bitsandbytes>=0.44.0
trl>=0.12.0
sentencepiece>=0.2.0
protobuf>=4.25.0
huggingface_hub>=0.24.0
